spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cursosb
    username: curso
    password: Admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update #Puede tener los valores: none, validate, update, create-drop. (By default: create-drop)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true



